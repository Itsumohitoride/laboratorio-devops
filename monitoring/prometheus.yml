# Configuracion de Prometheus para el laboratorio DevOps.
global:
  scrape_interval: 10s

scrape_configs:
  - job_name: app
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:3000

rule_files:
  - alerts.yml
